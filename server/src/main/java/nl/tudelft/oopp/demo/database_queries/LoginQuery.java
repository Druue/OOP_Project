package nl.tudelft.oopp.demo.database_queries;

import nl.tudelft.oopp.demo.entities.Quote;

import javax.naming.AuthenticationException;
import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;

public class LoginQuery {

    public static void userValidate(String NetID , String password) throws AuthenticationException {

        /*
        * Here goes the code that checks whether the user
        * exists in the database
        * If it does not, the method should throw new AuthenticationException()
        * */


    }
}
